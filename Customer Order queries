1~ Select and count all orders from January. Filter the results to only include data relevant to this query.
SELECT COUNT(distinct orderid)
FROM BIT_DB.JanSales
WHERE length(orderid) = 6
AND orderid <> 'Order ID';

2~ Using the query above, find out how many orders were for an iPhone.
SELECT COUNT(distinct orderid)
FROM BIT_DB.JanSales
WHERE length(orderid) = 6
AND orderid = 'iPhone'
AND orderid <> 'Order ID';

3~ Select the customer account numbers for all the orders that were placed in February.
SELECT distinct acctnum
FROM BIT_DB.customers cust

INNER JOIN BIT_DB.FebSales Feb
ON cust.order_id=FEB.orderid
WHERE length(orderid) = 6
AND orderid <> 'Order ID';

4~ Select the cheapest product sold in January and display the product name and price.
SELECT distinct product, price
FROM BIT_DB.JanSales
WHERE price in (SELECT min(price) FROM BIT_DB.JanSales)

5~ Show all products sold in January, and the amount of revenue generated by selling each product type.
SELECT product, sum(quantity)*price as revenue
FROM BIT_DB.JanSales
GROUP BY product
HAVING revenue>=1;

6~ Show all products that were sold in February at 548 Lincoln St, Seattle, WA 98101, how many of each were sold, and what the total revenue was.
SELECT
sum(Quantity),
product,
sum(quantity)*price as revenue
FROM BIT_DB.FebSales
WHERE location = '548 Lincoln St, Seattle, WA 98101'
GROUP BY product;

7~ Select how many customers ordered more than 2 products at a time in February, and what was the average amount spent for those customers.
SELECT count(distinct cust.acctnum), avg(quantity*price)
FROM BIT_DB.FebSales Feb
LEFT JOIN BIT_DB.customers cust
ON FEB.orderid=cust.order_id
WHERE Feb.Quantity>2
AND length(orderid) = 6
AND orderid <> 'Order ID';
